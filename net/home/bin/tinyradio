#!/usr/pkg/bin/bash

STREAM_FILE=~/.stream-list

[[ ! -f "$STREAM_FILE" ]] && echo "$STREAM_FILE does not exist" && exit 1

array=( $(cat "$STREAM_FILE") )

select opt in "${array[@]}"; do
    [[ $1 =~ ^-r ]] && streamripper "$opt" &
    mpg123 -C -b 2000 -@ "$opt"
    break
done
