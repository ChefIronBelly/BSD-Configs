#!/usr/bin/env bash

ARTIST=$( cmus-remote -Q 2>/dev/null | grep artist | cut -d " " -f 3- | head -n1)
TITLE=$( cmus-remote -Q 2>/dev/null | grep title | cut -d " " -f 3- )

if [ -z "$ARTIST" ]; then
    echo "Select-O-matic 100 | 2 Plays Quarter"
else
    echo "Now playing - $ARTIST - $TITLE"
fi
